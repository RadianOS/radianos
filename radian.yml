# This file will be auto generated by oskit

name: "RadianOS"
version: "0.1.0"
radian_version: "1.0"

kernel_entry: "system/core/kernel/main.rs"
bootloader: "boot/something"

structure:
  - `/system/core`          # Kernel and low-level runtime
  - `/system/lib`           # Base runtime libraries
  - `/system/include`       # Public headers and interfaces
  - `/system/run`           # Init processes and runtime daemons
  - `/user/binary`          # User-facing binaries
  - `/user/home`            # User home directories
  - `/devices`              # Device interface tree
  - `/mount`                # Filesystem mountpoints
  - `/mutable`              # Logs and stateful data
  - `/binary`               # Top-level static executables
  - `/temp`                 # Temp files

api_version: "1.0"

features:
  driver_model: "unified"
  ipc: "message-based"
  scheduler: "modular-rr"
  fs: "objfs"
  init_model: "event-driven"
  oskit_bootstrap: true

toolchain:
  arch: "i386"
  freestanding: true
  build_system: "oskit"
  cross_compile: "i386-elf-gcc"
  linker_script: "boot/loader.ld"

custom:
  branding: "RadianOS"
  security_model: "capability-based"
  ui_mode: "text-only"
  fallback_shell: "/binary/sh"
