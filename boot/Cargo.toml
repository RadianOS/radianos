[package]
name = "boot"
version = "0.0.2"
edition = "2024"

[dependencies]
panic-abort = "0.3.2"
uefi = { version = "0.35.0", features = ["alloc", "global_allocator", "logger", "panic_handler"] }
xmas-elf = "0.10.0"
